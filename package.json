{
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "turbo": "^1.6.3"
    },
    "scripts": {
		"build": "turbo run build",
		"dev": "turbo run dev --parallel",
		"lint": "turbo run lint"
	}
}
